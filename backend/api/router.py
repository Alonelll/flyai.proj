from fastapi import APIRouter
from computation.service import main

router = APIRouter()

# ToDo: Routes muss noch definiert werden,
# indem es sich mit der Datenbank verbindet und dann die Daten abruft und als JSON zurückgibt


@router.get("/detections")


    # fetch from database
    # return as json

